# Contribution scope

This branch is intended for upstream review as one focused security baseline. It does not include deployment-specific policy, terminal extraction, HMAC anti-replay, rate limiting or idempotency. Those remain separate follow-ups so each change can be reviewed and tested independently.
